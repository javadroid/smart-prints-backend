export enum TransactionStatus {
  PENDING = 'pending',
  ACTIVE='active',
  SUCCESS = 'success',
}

export enum TransactionType {
  DEPOSIT = 'deposit',
  WITHDRAWAL = 'withdrawal',
  PAYMENT = 'payment',
  REFUND = 'refund',
  TRANSFER = 'transfer',
}
